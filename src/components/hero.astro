---
import { Image } from "astro:assets";
import heroImage from "@/assets/hero.png";
import Link from "@/components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main class="grid md:grid-cols-2 gap-8 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
      Embed. Sandbox. Ship.
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl">
      A single library for AI agent isolation. No daemon, no dependencies—just import and sandbox.
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="https://github.com/boxlite-labs/boxlite-python-examples"
        target="_blank"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="text-white w-5 h-5" name="bx:bxs-rocket" />
        Get Started
      </Link>
      <span
        class="flex gap-1 items-center justify-center px-5 py-2.5 rounded-md border-2 border-gray-300 text-gray-400 cursor-not-allowed">
        <Icon class="w-4 h-4" name="bx:bxl-github" />
        Coming Soon
      </span>
    </div>
  </div>
  <div class="hidden md:flex justify-center">
    <Image
      src={heroImage}
      alt="BoxLite"
      width={280}
      height={280}
      class="w-[280px] h-auto"
    />
  </div>
</main>
