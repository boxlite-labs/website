---
import { Picture } from "astro:assets";
import heroImage from "@/assets/hero.png";
import Link from "@/components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main class="flex flex-col items-center text-center pt-16 pb-8 md:pt-20 md:pb-24">
  <!-- Primary H1 -->
  <h1
    class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter">
    Embed. Sandbox. Ship.
  </h1>

  <!-- TL;DR style hook for AI citation - direct answer format -->
  <p class="text-xl mt-6 text-slate-700 max-w-2xl font-medium">
    BoxLite is an embeddable micro-VM runtime that runs untrusted code in hardware-isolated sandboxes.
  </p>

  <!-- Expanded description with key differentiators -->
  <p class="text-lg mt-3 text-slate-500 max-w-2xl">
    Unlike Docker containers that share the host kernel, each BoxLite sandbox runs its own kernel—making escape impossible.
    No daemon, no root access needed. Just <code class="bg-slate-100 px-1.5 py-0.5 rounded text-sm">pip install boxlite</code> and sandbox.
  </p>

  <!-- Key stats/facts for AI citation -->
  <div class="flex flex-wrap justify-center gap-6 mt-8 text-sm text-slate-600">
    <div class="flex items-center gap-2">
      <Icon class="w-5 h-5 text-green-600" name="bx:bx-check-circle" />
      <span>Sub-second boot</span>
    </div>
    <div class="flex items-center gap-2">
      <Icon class="w-5 h-5 text-green-600" name="bx:bx-check-circle" />
      <span>OCI/Docker compatible</span>
    </div>
    <div class="flex items-center gap-2">
      <Icon class="w-5 h-5 text-green-600" name="bx:bx-check-circle" />
      <span>macOS & Linux</span>
    </div>
    <div class="flex items-center gap-2">
      <Icon class="w-5 h-5 text-green-600" name="bx:bx-check-circle" />
      <span>Python, Rust, C SDKs</span>
    </div>
  </div>

  <!-- CTAs -->
  <div class="mt-8 flex flex-col sm:flex-row gap-3">
    <Link
      href="https://github.com/boxlite-labs/boxlite-python-examples"
      target="_blank"
      class="flex gap-1 items-center justify-center"
      rel="noopener">
      <Icon class="text-white w-5 h-5" name="bx:bxs-rocket" />
      Get Started
    </Link>
    <Link
      size="lg"
      style="outline"
      rel="noopener"
      href="https://github.com/boxlite-labs/boxlite"
      target="_blank"
      class="flex gap-1 items-center justify-center">
      <Icon class="text-black w-4 h-4" name="bx:bxl-github" />
      GitHub
    </Link>
  </div>

  <!-- Code snippet for developers - helps with technical SEO -->
  <div class="mt-12 w-full max-w-xl">
    <div class="bg-slate-900 rounded-lg p-4 text-left">
      <div class="flex items-center gap-2 mb-3">
        <div class="w-3 h-3 rounded-full bg-red-500"></div>
        <div class="w-3 h-3 rounded-full bg-yellow-500"></div>
        <div class="w-3 h-3 rounded-full bg-green-500"></div>
        <span class="text-slate-400 text-xs ml-2">Python</span>
      </div>
      <pre class="text-sm text-slate-300 overflow-x-auto"><code><span class="text-purple-400">import</span> boxlite

<span class="text-purple-400">async with</span> boxlite.SimpleBox(<span class="text-green-400">"python:slim"</span>) <span class="text-purple-400">as</span> box:
    result = <span class="text-purple-400">await</span> box.exec(<span class="text-green-400">"python"</span>, <span class="text-green-400">"-c"</span>, <span class="text-green-400">"print('Hello!')"</span>)
    <span class="text-purple-400">print</span>(result.stdout)  <span class="text-slate-500"># Hello!</span></code></pre>
    </div>
  </div>
</main>
